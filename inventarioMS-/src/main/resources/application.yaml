spring:
  application:
    name: inventarioMS
  datasource:
    url: jdbc:mysql://localhost:3306/inventario_db
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8083

eureka:
    client:
        service-url:
            defaultZone: http://localhost:8761/eureka/