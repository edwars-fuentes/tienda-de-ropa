spring:
  application:
    name: ServicioReportesMS
  datasource:
    url: jdbc:mysql://localhost:3306/reportes_db
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8089

eureka:
    client:
        service-url:
            defaultZone: http://localhost:8761/eureka/