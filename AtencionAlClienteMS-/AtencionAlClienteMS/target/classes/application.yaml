spring:
  application:
    name: AtencionAlClienteMS
  datasource:
    url: jdbc:mysql://localhost:3306/atencion_db
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8090

eureka:
    client:
        service-url:
            defaultZone: http://localhost:8761/eureka/