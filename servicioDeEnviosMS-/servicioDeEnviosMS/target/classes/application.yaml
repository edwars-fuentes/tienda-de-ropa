spring:
  application:
    name: servicioDeEnviosMS
  datasource:
    url: jdbc:mysql://localhost:3306/envios_db
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8087

eureka:
    client:
        service-url:
            defaultZone: http://localhost:8761/eureka/