spring:
  application:
    name: servicioDePagoMS
  datasource:
    url: jdbc:mysql://localhost:3306/pagos_db
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8085

#apuntamos donde esta el servidor eureka para que el microservicio se haga cliente
eureka:
    client:
        service-url:
            defaultZone: http://localhost:8761/eureka/