spring:
  application:
    name: pedidosMS
  datasource:
    url: jdbc:mysql://localhost:3306/pedidos_db
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8086

eureka:
    client:
        service-url:
            defaultZone: http://localhost:8761/eureka/