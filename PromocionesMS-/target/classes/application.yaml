spring:
  application:
    name: promocionesMS
  datasource:
    url: jdbc:mysql://localhost:3306/promociones_db
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8084

eureka:
    client:
        service-url:
            defaultZone: http://localhost:8761/eureka/